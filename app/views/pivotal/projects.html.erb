<h1>Gerard's Tracker Projects</h1>

<ul>
  <% @project_data.each do |project| %>
    <li><%= link_to project["name"], view_stories_path(project["id"]) %></li>
  <% end %>
</ul>

<%= link_to "Home", root_path %>
